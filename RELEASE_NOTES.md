<!-- 发版前请将本文件内容替换为「当版」说明；若留空或删除本文件，CI 会自动回退为 Full Changelog 链接。 -->

# WeKnora Mobile v1.2.9

**Full Changelog**: https://github.com/lichong0429/weknora-mobile/compare/v1.2.8...v1.2.9

## 新功能

- **对话中切换模型**：聊天页配置面板新增模型选择下拉（KnowledgeQA 模型），支持指定本次对话使用的 LLM。
- **智能体测试对话**：智能体详情页新增「测试对话」按钮，一键创建测试会话验证 Agent 效果。

## 优化

- **首页精简**：首页仅保留「知识库 / 搜索 / 会话 / 智能体」四个核心入口，其余管理功能（模型 / 向量库 / 搜索引擎 / 系统 / 诊断）统一收纳在设置页。
- **底部导航不再遮挡**：修复各页面内容被底部导航栏遮挡、滑动看不到全部的问题。

## 修复

- 聊天页从会话自动回填智能体与模型选择（`session.agent_id` / `session.model_id`）。

## 版本号

- 应用版本号统一更新为 **1.2.9**（package.json / 诊断页 APP_VERSION / APK versionName 同步修正，此前一直停留在 1.2.5）。

## 校验
- 4 个代码文件 + 版本号改动通过 esbuild 全量打包校验。
