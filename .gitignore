# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
dist-webview
dist-ssr
*.local

# Download page deployment directory
dist-download

# Root APK deliverables (these go to GitHub Releases, not source repo)
/weknora-mobile.apk
/weknora-mobile-webview.apk

# Embedded PWA assets (generated from dist-webview)
/webview-app/app/src/main/assets

# Android SDK (downloaded locally)
.android-sdk

# Gradle user home used for offline builds
.gradle-home

# APK signing / intermediate
#
# ⚠️ 已知例外（安全债）：android-ci/ci.keystore 曾被 `git add -f` 强制提交，
#    绕过了下面的规则；其口令也明文写在 webview-app/app/build.gradle 中。
#    本仓库为 PUBLIC，等于签名密钥与口令同时公开 —— 任何人都能签出「同签名」APK。
#    任何新增密钥/口令文件都不要提交；轮换前请勿删除该文件，否则 CI 会构建失败。
*.jks
*.keystore
/android/app/build
/android/.gradle
/android/local.properties
/android/*.apk
/android/*.aab
/android/*.idsig

# WebView app build outputs
/webview-app/app/build
/webview-app/build
/webview-app/.gradle
/webview-app/local.properties

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*

# Vite build temp files
*.timestamp-*.mjs
